start_number = 5
countdown_values = []
current = start_number
while current >= 1:
    countdown_values.append(current)   # fixed typo: append, not appened
    current -= 1
print("Discount countdown complete!")
print(countdown_values)